#!/usr/bin/env bash
set -euo pipefail

script_directory="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
cd "$script_directory"
exec dotnet run --project "$script_directory/src/build/Merkle.Build.csproj" -- "$@"
